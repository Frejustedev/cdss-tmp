# Exemple de configuration des secrets pour le CDSS-TMP.
# Copier ce fichier en .streamlit/secrets.toml et remplir avec ta vraie
# chaîne de connexion Supabase (projet ScintiFlow).
#
# NE JAMAIS commiter le vrai secrets.toml (il est dans .gitignore).
#
# Récupérer l'URI : Dashboard Supabase → ScintiFlow → Connect → URI
# (mode Transaction pooler recommandé pour Streamlit Cloud).

SUPABASE_DB_URL = "postgresql://postgres.zhvflbydddorvovjvkts:TON_MOT_DE_PASSE@aws-0-eu-central-2.pooler.supabase.com:6543/postgres"
